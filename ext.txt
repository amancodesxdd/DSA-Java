for(int i = 0; i < arr.length-1;i++){
            for(int k = 0; k < arr.length-1;k++){
                if(arr[k] == 0){
                        int temp = arr[k];
                        arr[k] = arr[k + 1];
                        arr[k + 1] = temp;
            }
        }
    }
}